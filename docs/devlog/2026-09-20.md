# DevLog — 2026-09-20

## Public Interaction Summary — Mobile Intelligence M1.5

- **Timestamp:** 2026-09-20 (Asia/Shanghai)
- **Stage:** Mobile Intelligence M1.5 environment validation
- **User Goal:** Prove the real Android observation path before allowing verified navigation.
- **What Codex inspected:** Product baseline, mobile contracts, Tauri Android adapter, typed IPC, persistence, UI diagnostics, tests, and local Android tool availability.
- **What Codex changed:** Added ordered SDK/tool discovery, existing-AVD lifecycle checks, command deadlines, production capture projection, persisted session shutdown, Settings diagnostics, and the ten-check smoke gate.
- **Files changed:** Mobile runtime contracts, Tauri mobile adapter and commands, application persistence, TypeScript contracts/UI, tests, and mobile documentation.
- **Problems discovered:** No Android SDK/ADB/Emulator was installed; review also found missing process bounds, inconsistent environment selection, and acceptance checks that did not initially traverse enough of the production path.
- **Solution:** Fail closed on the missing environment; make subprocesses bounded; share one environment resolver; require persistence, audit events, workspace projection, serialization, and persisted shutdown for acceptance.
- **Commands and tests executed:** Rust formatting, Clippy with warnings denied, full Rust and TypeScript suites, typecheck, Vite build, Tauri bundle build, and explicit real Android smoke.
- **Result:** 126 Rust and 31 TypeScript tests passed at product baseline `c4f2d66`; typecheck and builds passed. The real smoke reported three failed prerequisites and seven blocked checks. M2 was not entered.
- **Remaining risks:** The real Android capture and shutdown path remains unverified until the external environment exists.
- **Next recommended step:** Install or expose a real Android toolchain and one safe AVD, then rerun the same gate unchanged.
- **Codex engineering note:** The useful outcome was not a green badge; it was a gate that refused to confuse fixture coverage with real integration evidence.

## Public Interaction Summary — Open development system

- **Timestamp:** 2026-09-20 (Asia/Shanghai)
- **Stage:** Public engineering record and publication automation
- **User Goal:** Establish sanitized DevLogs and GitHub synchronization; prepare stage-level X content for manual owner publication only.
- **What Codex inspected:** Current code and docs, Git history, branch/remote state, tracked files, existing tests, project instructions, and sensitive-path exposure.
- **What Codex changed:** Added the public documentation set, interaction-summary policy, fail-closed sanitizer, allowlisted Git sync, tested macOS scheduler installer, two-hour Codex heartbeat, and a six-post manual X queue. X publishing automation was removed after the owner retained publication responsibility.
- **Files changed:** `docs/open-source/`, `docs/devlog/`, README, project instructions, public-log scripts/tests, the manual X queue, and existing text fixtures that contained raw local home paths.
- **Problems discovered:** The checkout has no Git remote. A standalone LaunchAgent also received macOS exit 126 because its background shell could not access this checkout under `Documents`. Fresh default-parallel Rust tests exposed two AVD lifecycle timeouts that pass serially.
- **Solution:** Use repository-backed truth labels, replace local paths with portable references, fail closed before staging, retain local commits when push fails, remove the inaccessible LaunchAgent, and schedule the same sync script through a two-hour Codex heartbeat. The product-test timing issue is recorded without changing Mobile Runtime scope.
- **Commands and tests executed:** Shell syntax checks; sanitizer, sync, scheduler, document-schema, plist, privacy, and diff checks; Rust formatting and Clippy; default-parallel and serial Rust tests; TypeScript tests and typecheck; Vite production build; and Tauri desktop bundle build.
- **Result:** Public-log checks passed, 31 TypeScript tests passed, and the web and desktop builds succeeded. Rust formatting and Clippy passed; the default-parallel desktop unit run failed two of 20 tests, while the same 20 passed serially. The two-hour heartbeat is active. GitHub push is blocked with `GITHUB_REMOTE_REQUIRED`; X was not opened or published.
- **Remaining risks:** An existing OrdinConn GitHub repository must be configured as `origin` before the first push. The two AVD timeout tests still need a product-scope concurrency fix. The real Android M1.5 environment remains unavailable.
- **Next recommended step:** Configure the existing OrdinConn GitHub repository as `origin`, then run the fail-closed sync script unchanged.
- **Codex engineering note:** Build-in-public records need provenance and redaction gates just as financial Signals need Evidence gates.

## Public Interaction Summary — GitHub open-source initialization

- **Timestamp:** 2026-09-20 (Asia/Shanghai)
- **Stage:** Official public repository and long-term Build in Public foundation
- **GitHub Issue:** [#2 — Establish Public Development Logging and GitHub Source of Truth](https://github.com/Btkkgo/OrdinConn/issues/2)
- **User Goal:** Make GitHub the complete public engineering source of truth, keep X manual, and synchronize every publishable project artifact without exposing private data.
- **What Codex inspected:** Local branches and history, tracked files, license metadata, GitHub authentication and repository existence, README/docs, ignore rules, commit emails, raw home paths, build artifacts, runtime tests, and scheduler behavior under macOS privacy controls.
- **What Codex changed:** Created the official public repository; normalized the default branch to `main`; added bilingual README, MIT License, contribution/security/changelog files, Issues, 19 labels, templates, Current Status, ADRs, documentation indexes, manual X draft, history-aware security gate, and allowlisted two-hour sync. The scheduled sync validates every unpushed commit, rejects workflow files and mismatched push URLs, and scans the staged snapshot before commit. X authentication and publication remain disabled.
- **Files changed:** Public repository entry files, `AGENTS.md`, `.github/`, `docs/`, `social/x/`, `scripts/github/`, `scripts/public-log/`, `scripts/security/`, and `.gitignore`. Product runtime code was not changed.
- **Problems discovered:** Git history exposed a private commit email and old macOS home prefixes; the first SSH push lacked a usable public key; standalone launchd execution could not read the checkout under `Documents`; and two one-second AVD lifecycle tests fail under default Rust suite concurrency. Final review also found that an earlier sync design could carry prior product commits, stage Actions workflows, miss lowercase credential assignments, race between scan and stage, or honor a mismatched Git push URL.
- **Solution:** Backed up Git history locally, rewrote only public metadata and home prefixes, switched the official remote to HTTPS through existing GitHub CLI credentials, removed the failing LaunchAgent, retained a tested installer/template, and used a Codex heartbeat for the active two-hour trigger. The sync now checks each unpushed commit, narrows GitHub metadata paths, detects lowercase credentials, scans and restores the staged snapshot on failure, and validates every push URL. The product-test timing behavior was recorded without changing Mobile Runtime scope.
- **Commands and tests executed:** Repository audit, GitHub repository/label/Issue creation, shell syntax, sanitizer and history-gate tests, allowlist sync tests, document/link validation, plist lint, `git diff --check`, full security gate, Rust format/Clippy/tests, TypeScript tests/typecheck, Vite build, Tauri bundle, remote file verification, and Issue/repository queries.
- **Result:** `https://github.com/Btkkgo/OrdinConn` is public with `main`, MIT License, Issues enabled, source and documentation present, and three scoped Issues. The security gate passed and the initial push succeeded. All 31 TypeScript tests, typecheck, web build, and desktop bundle passed. Rust format and Clippy passed; default-parallel Rust tests failed two desktop AVD lifecycle cases, while all 20 desktop library tests passed serially. M1.5 remains NOT PASSED and M2 remains NOT STARTED.
- **Remaining risks:** The two AVD test timeouts need a product-scope fix. Real Android validation remains blocked. The standalone LaunchAgent remains unusable for this protected checkout, so the active schedule depends on the Codex heartbeat.
- **Next recommended step:** Continue [Issue #1](https://github.com/Btkkgo/OrdinConn/issues/1) by establishing the real Android Emulator runtime and rerunning the complete M1.5 gate.
- **Codex engineering note:** The most important open-source step was not creating a repository; it was proving that the repository history itself was safe to publish and preserving blocked validation as first-class engineering evidence.

## Public Interaction Summary — Persistent macOS GitHub sync

- **Timestamp:** 2026-09-20 (Asia/Shanghai)
- **Stage:** GitHub Public Sync system-level scheduling repair
- **GitHub Issue:** [#2 — Establish Public Development Logging and GitHub Source of Truth](https://github.com/Btkkgo/OrdinConn/issues/2)
- **User Goal:** Make the two-hour public GitHub sync run independently of Codex and ChatGPT while preserving the documentation-only security boundary.
- **What Codex inspected:** The previous plist, installer, sync allowlist, credential helper, launchd state, structured logs, macOS TCC events, and the exact background process attribution.
- **What Codex changed:** Moved the runner and working directory into Application Support; added a dedicated, ad-hoc-signed `OrdinConn GitHub Sync` launcher; retained the repository-owned fail-closed sync logic; added explicit tool paths, structured status fields, 5 MiB log rotation, and installer tests; and kept X publishing outside automation.
- **Files changed:** macOS GitHub sync installer, uninstaller, runner/plist templates, dedicated launcher source and metadata, tests, Current Status, Build in Public documentation, Issue record, and this DevLog.
- **Problems discovered:** Moving only the shell script was insufficient. TCC logs proved that `/bin/bash` remained the responsible process and was denied `Documents` access. The installed Command Line Tools SDK also failed to link on macOS 27, so the installer had to select the active full-Xcode compiler and SDK explicitly.
- **Solution:** Give the narrow OrdinConn launcher its own macOS privacy identity, keep the shell runner outside `Documents`, and let the launcher spawn the runner. Do not grant broad access to Terminal or `/bin/bash`. Use Git's `osxkeychain` credential helper rather than placing a token in the plist, runner, or repository.
- **Commands and tests executed:** Shell syntax checks; installer rendering/compile/uninstall test; allowlisted sync integration test; sanitizer and public-repository security tests; public document validation; `plutil`; `codesign`; `launchctl bootstrap`, `print`, and `kickstart`; TCC log inspection; remote commit verification; and a second no-change run.
- **Result:** The primary scheduler is `com.ordinconn.github-sync`, runs at load and every 7,200 seconds, and has no Codex dependency. Its structured log records repository, branch, scan, changes, commit, push, error, and status without credentials. The final real change/push and no-change evidence are posted to Issue #2.
- **Remaining risks:** The launcher has an ad-hoc local signature, so reinstalling or replacing its binary may require macOS Documents permission to be granted again. Product runtime and Mobile M1.5 were not changed.
- **Next recommended step:** Leave the LaunchAgent as primary and keep the Codex heartbeat paused unless it is intentionally used as a recovery fallback.
- **Codex engineering note:** macOS privacy attribution follows the responsible executable, not merely the location of the shell script; a narrow identity is safer and more reliable than granting a general-purpose interpreter broad access.
