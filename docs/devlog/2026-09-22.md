# DevLog — 2026-09-22

[English](2026-09-22.md) | [简体中文](2026-09-22.zh-CN.md)

## Public Interaction Summary — Issue #7 sanitized receipts and audit (in progress)

- **Timestamp:** 2026-09-22 (Asia/Shanghai)
- **Stage:** M2 persistence and post-Type privacy; real AVD and UI acceptance not yet run.
- **User Goal:** Ensure every attempted action has a sanitized, persisted receipt and audit trail while Type plaintext never enters stored post-action observations.
- **Inspected areas:** SQLite mobile migrations, session/snapshot/observation inserts, append-only runtime events, workspace projection, Android post-capture path, and fake-ADB Type behavior.
- **Changes and files:** Added migration `0006_mobile_action_receipts.sql`; `ordinconn-app` now atomically stores receipts, optional sanitized post-capture, and requested/blocked/executed/verified audit events, and projects the latest receipt. The Android Host redacts editable fields and matching Type text before returning post-capture, regenerates the observation hash, and reserves action budget before an ambiguous ADB command. Added focused app and host tests.
- **Problems and solution:** A real typed query can reappear in a post-action UI tree even when request/receipt serialization excludes it. A failing fake-ADB test reproduced that leak; the adapter now removes typed text from the semantic capture before persistence, while frames remain memory-only. Duplicate action IDs now roll back receipt and post-capture inserts in one transaction.
- **Commands and tests:** RED→GREEN post-Type capture test; `cargo test -p ordinconn-desktop mobile_action` 7/7 PASS; `cargo test -p ordinconn-app mobile_action` 2/2 PASS; `cargo clippy -p ordinconn-app --all-targets -- -D warnings` PASS; `cargo fmt --all -- --check` PASS; `scripts/security/check-public-repo.sh` PASS; `git diff --check` PASS.
- **Result:** Persistence and privacy behavior are fixture-verified. Typed IPC, React controls, real AVD Settings navigation, full regressions, and M2 acceptance remain NOT RUN. M2 remains in progress.
- **Remaining risks and next step:** The adapter is still unreachable from UI; the real launcher and Settings flow must be tested after typed IPC and manual controls are integrated.
- **Codex engineering note:** Redacting the request alone is insufficient; the post-observation is a second pathway by which an input value can become durable data.

## Public Interaction Summary — Issue #7 guarded Android adapter (in progress)

- **Timestamp:** 2026-09-22 (Asia/Shanghai)
- **Stage:** M2 adapter fixture verification; no real device action or user-facing action path yet.
- **User Goal:** Continue the same six-action, fail-closed Verified Navigation implementation.
- **Inspected areas:** Existing `MobileHost` observation and locks, bounded ADB process runner, focus parser, fake-ADB fixtures, platform-neutral policy, and Android package launcher resolution.
- **Changes and files:** `crates/mobile-runtime/src/action.rs` now requires an already-focused Type target and bounds screen dimensions. `apps/desktop/src-tauri/src/mobile.rs` now serializes observe/stop/action operations, caches the last capture, enforces the 20-action budget, rechecks live foreground and device identity, invokes fixed action verbs, and performs a new production capture after execution. README and Current Status are synchronized in both languages. The adapter is intentionally not yet reachable through IPC.
- **Problems and solution:** The first fake-ADB post-capture test showed that a different emulator could otherwise be mistaken for the same session. Added a RED→GREEN device-ID mismatch gate that marks the attempt failed/unexpected and discards the foreign capture. An implausibly large screen could overflow swipe arithmetic; it now fails policy before coordinate calculation. The fake ADB test log redacts Type text.
- **Commands and tests:** `cargo test -p ordinconn-desktop mobile_action` 6/6 PASS, `cargo test -p mobile-runtime action::tests` 7/7 PASS, `cargo clippy -p ordinconn-desktop --all-targets -- -D warnings` PASS, `cargo fmt --all -- --check` PASS, `scripts/security/check-public-repo.sh` PASS, `git diff --check` PASS.
- **Result:** Adapter behavior is fixture-verified only. Persistence, typed IPC, React controls, real AVD flow, and M2 acceptance remain NOT RUN. M2 remains in progress and not verified.
- **Remaining risks and next step:** The adapter is not production-wired; launcher resolution and post-action behavior still require dedicated AVD proof. Implement sanitized SQLite receipts and audit before exposing a manual IPC action.
- **Codex engineering note:** Device identity is part of verification lineage; a visually identical frame from a different emulator must never count as a valid post-action observation.

## Public Interaction Summary — Issue #7 M2 domain foundation (in progress)

- **Timestamp:** 2026-09-22 (Asia/Shanghai)
- **Stage:** Mobile Intelligence M2 — Verified Navigation, implementation in progress; no device action executed yet.
- **GitHub Issue:** [#7 — Mobile Intelligence M2 — Verified Navigation and Action Receipts](https://github.com/Btkkgo/OrdinConn/issues/7)
- **User Goal:** Add six bounded, manual emulator navigation actions with fail-closed policy, sanitized receipts, post-action observation, and real acceptance; no autonomous Agent loop, M3, or X publication.
- **What Codex inspected:** Product baseline, current main and remote, M1.5 mobile runtime/host, SQLite and event interfaces, typed IPC and React Live View, existing mobile security and action protocol.
- **What Codex changed:** After matching local main, origin/main, and GitHub main, deleted only the authorized local privacy-repair backup ref; the full local public-repository security gate then passed. Created bilingual Issue #7 and its issue branch. Added a paired implementation plan and platform-neutral action target, request, policy decision, sanitized receipt, and verification contracts with focused policy tests.
- **Files changed:** `crates/mobile-runtime/src/action.rs`, `crates/mobile-runtime/src/lib.rs`, the paired M2 implementation plan, and this paired DevLog. The local backup-ref deletion is not a repository file change.
- **Problems discovered:** The prior local-only backup ref caused the all-ref security gate to fail despite repaired public main; its exact authorized removal resolved the gate. M1.5 had no action-domain contract, as expected.
- **Solution:** Use an exact old-SHA local `update-ref -d` with no other ref, GC, reflog, history, or remote changes. Keep Type plaintext in an ephemeral request field excluded from serialization and redacted from Debug; policy rejects unsafe input and stale/sensitive targets before any adapter exists.
- **Commands and tests:** `git fetch origin`, exact main/remote/GitHub SHA checks, local backup-ref check and deletion, `scripts/security/check-public-repo.sh` PASS, RED→GREEN `cargo test -p mobile-runtime action::tests -- --nocapture` (6 PASS), `cargo fmt --all -- --check` PASS, and `cargo clippy -p mobile-runtime --all-targets -- -D warnings` PASS.
- **Result:** Privacy cleanup PASS; M2 domain foundation implemented and unit-tested. Android action adapter, persistence, IPC/UI, real navigation, and complete acceptance remain NOT RUN. M2 is not complete.
- **Remaining risks:** The safe Type contract currently accepts ASCII letters, digits, and spaces only; broader Unicode input is rejected rather than risking unsafe ADB encoding. No real action has been performed.
- **Next step:** Add the Android adapter behind the same policy and test no-side-effect denial before enabling any manual UI control.
- **Codex engineering note:** Snapshot Element Refs are only meaningful inside their exact capture; a policy test must prove stale Ref rejection before the host can translate one to a physical Tap.

## Public Interaction Summary — Issue #4 parallel runtime fixtures

- **Timestamp:** 2026-09-22 (Asia/Shanghai)
- **Stage:** Runtime reliability and test infrastructure; Mobile M2 not started.
- **GitHub Issue:** [#4 — Rust AVD lifecycle and subprocess tests fail under parallel execution](https://github.com/Btkkgo/OrdinConn/issues/4)
- **User Goal:** Identify and remove the intermittent default-parallel test failure without weakening production deadlines or revisiting M1.5 acceptance.
- **What Codex inspected:** Current `main`, Issue #4 and its historical failures, product baseline, process/AVD helpers, pipe and process-group lifecycle, fixture temporary paths, environment/global state, timeout behavior, related tests, real Android device state, and public documentation policy.
- **What Codex changed:** Added one four-worker concurrent AVD regression with independent SDK/ADB/Emulator/AVD fixtures and controlled cold-start latency; separated bounded success-path fixture budgets from deliberate short timeout tests; updated the long-term GitHub-versus-X publication rule and synchronized bilingual public records. Production runtime code and deadlines were not changed.
- **Files changed:** `AGENTS.md`, desktop mobile tests, English/Chinese Build in Public and X policy pages, Current Status, Issue #4 record, Problems and Solutions, Codex Field Notes, and this paired DevLog. No new X draft was created.
- **Problems discovered:** The first of ten pre-change default-parallel desktop runs failed the same four cases at 24/28, while nine warm runs passed; each case passed 20 isolated runs. A success-path one-second fixture budget conflated cold external-process startup with functional behavior. The first real Android smoke correctly rejected an unallowlisted Launcher after cold boot. The local all-ref security gate also includes an intentionally retained, local-only privacy-repair backup outside this branch.
- **Solution:** Use two-second test-only budgets for successful large-output/inherited-pipe cases and three seconds for successful AVD lifecycle cases; retain the 30 ms command-timeout and 50 ms no-boot tests, production timeouts, process-group cleanup, and default parallel execution. Open Settings on the dedicated AVD, then rerun the same allowlisted smoke without broadening its allowlist.
- **Commands and tests executed:** Ten pre-change default-parallel desktop runs; 20 isolated runs of each of the four named cases; 20 eight-thread and 20 32-thread warm related-test runs; RED→GREEN concurrent cold-start regression; 20 post-change default-parallel desktop runs; ten full default-parallel workspace runs; one eight-thread desktop run; real Android smoke before and after setting the foreground app; `cargo fmt --all -- --check`; Clippy with warnings denied; Rust workspace build; 31 TypeScript tests; TypeScript typecheck; Vite build; Tauri macOS bundle; public-document validator; working-tree sanitizer and current-branch identity scan; isolated public-branch security gate; and `git diff --check`.
- **Result:** The new regression failed under the original one-second budget and passed after the test-only change. Post-change desktop passed 20/20 runs at 29/29 tests each; workspace passed 10/10 runs at 136 tests each; eight-thread desktop passed 29/29. The first Android smoke reported `FRAME_CAPTURE=FAIL` and downstream `BLOCKED` because the foreground Launcher was not allowed; the later unchanged smoke on Settings passed all ten checks, including frame, UI tree, `MobileObservation`, workspace projection, and logical shutdown. Rust/TypeScript static checks and builds passed. The local all-ref security gate reported the retained backup; the current branch and working tree passed targeted checks, and the complete gate passed in isolated checkouts of both the issue branch and merged `main`. PR #6 merged as `52e73ca`, and Issue #4 closed. M1.5 remains 15 PASS / 0 FAIL / 0 BLOCKED / 0 NOT RUN; M2 remains NOT STARTED.
- **Remaining risks:** The new synthetic cold-start regression makes the desktop suite about two seconds slower than the previous warm roughly 0.5-second run. Real-world scheduling can vary beyond any finite test budget; the exact historical failures remain in the Issue record. The operator-owned AVD was not terminated by logical session shutdown.
- **Next recommended step:** Consider Mobile Intelligence M2 — Verified Navigation only in a separately authorized task.
- **Codex engineering note:** A green rerun is not a deterministic test. Controlled latency plus repeated default-parallel runs exposed an accidental performance assertion in tests without changing production safety boundaries.
